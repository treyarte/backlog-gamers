import { Metadata } from "next"

 const metadata:Metadata = {
    title: "About",
    description: "HELLO"
}

export default function About() {
    return (
        <div>
            About
        </div>
    )
}