import { Metadata } from "next"

export const metadata:Metadata = {
    title: "About",
    description: "HELLO"
}

export default function About() {
    return (
        <div>
            About
        </div>
    )
}